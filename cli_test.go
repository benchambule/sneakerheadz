package main

func test() {
	// client_name := "João"
	// product_name := "Nike Air Max"
	// product_price := 1000.0
	// fifty_percent := product_price / 2
	// now := time.Now()

	// request := &bot.Request{
	// 	Msisdn: "258849902174",
	// 	Prompt: "1",
	// 	Parameters: map[string]string{
	// 		"{{client_name}}":   client_name,
	// 		"{{product_name}}":  product_name,
	// 		"{{product_price}}": fmt.Sprintf("%.2f", product_price),
	// 		"{{fifty_percent}}": fmt.Sprintf("%.2f", fifty_percent),
	// 		"{{book_date}}":     now.Format("02/01/2006"),
	// 		"{{delivery_date}}": now.Add(14 * 24 * time.Hour).Format("02/01/2006"),
	// 	},
	// }

	// menu := bot.ProcessRequest(request)
	// fmt.Println(menu.Name, menu.Body)

	// menu = bot.ProcessRequest(request)
	// fmt.Println(menu.Name)

	// menu = bot.ProcessRequest(request)
	// fmt.Println(menu.Name)

	// request.Prompt = "2"
	// menu = bot.ProcessRequest(request)
	// fmt.Println(menu.Name)

	// request.Prompt = "1"
	// menu = bot.ProcessRequest(request)
	// fmt.Println(menu.Name)

	// menu = bot.ProcessRequest(request)
	// fmt.Println(menu.Name)

	// request.Prompt = "3"
	// menu = bot.ProcessRequest(request)
	// fmt.Println(menu.Name)

	// menu = bot.ProcessRequest(request)
	// fmt.Println(menu.Name)

	// request.Prompt = "4"
	// menu = bot.ProcessRequest(request)
	// fmt.Println(menu.Name)

	// request.Prompt = "1"
	// menu = bot.ProcessRequest(request)
	// fmt.Println(menu.Name)
}
